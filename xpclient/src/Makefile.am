# The Program name to build
bin_PROGRAMS=xpclient

# The Sources needed to build the programs
xpclient_SOURCES = xpclient.c serverdata.c handleserver.c xplanebeacon.c

# The Headers needs to build the programs
noinst_HEADERS= xpclient.h serverdata.h handleserver.h common.h xplanebeacon.h

